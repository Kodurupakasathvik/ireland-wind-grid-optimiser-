[pytest]
# The repository keeps several historical diagnostic programs named
# ``*_test.py``. They are executable analyses that write result files, not
# pytest modules. Restrict discovery to the conventional unit-test naming
# pattern so collection is side-effect free.
testpaths = scripts tests
python_files = test_*.py
